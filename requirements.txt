# ─────────────────────────────────────────────────────────
# Book Recommender System — Requirements
# Tested on Streamlit Cloud (Python 3.9 / 3.10 / 3.11)
# ─────────────────────────────────────────────────────────

# Core framework
streamlit==1.35.0

# Data handling (pickle files contain pandas DataFrames)
pandas==2.0.3

# Numerical arrays inside .pkl similarity matrix
numpy>=1.24.0,<2.0.0

# Required to UNPICKLE the similarity_score.pkl if it was
# created with sklearn objects (e.g. cosine_similarity output)
scikit-learn==1.3.0
